import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root'
})
export class LoginService {

  constructor() { }

  // isLoggedIn(user: string, pass: string): boolean {
  //   let cliente = this.clientes.find(c => c.name == user && c.password == pass);
  //   if (!cliente) {
  //     return false;
  //   } else {
  //     return true;
  //   }
  // }

}
